# API base URL (optional in dev — defaults to http://localhost:5001)
VITE_API_URL=

# Google OAuth Web Client ID — must match GOOGLE_OAUTH_CLIENT_ID on the server.
# Create credentials at https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_OAUTH_CLIENT_ID=
