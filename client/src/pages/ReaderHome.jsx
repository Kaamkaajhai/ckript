import InvestorDeskPage from "../features/investor-desk";

/*
 * /reader renders the same desk as /home. The page names the viewer's own
 * workspace in its masthead, so one implementation serves both audiences.
 */
export default InvestorDeskPage;
