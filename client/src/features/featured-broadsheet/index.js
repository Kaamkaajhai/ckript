/*
 * featured-broadsheet — /featured, for every audience that can reach it.
 *
 * The route is shared: industry professionals, actors and readers all land
 * here, which is why this is its own feature module rather than living inside
 * producer-workspace. The page adapts to the viewer (the mandate shelf and the
 * toolbar's primary action) instead of forking into per-audience copies.
 */
export { default } from "./FeaturedProjectsPage";
export { default as FeaturedProjectsPage } from "./FeaturedProjectsPage";
